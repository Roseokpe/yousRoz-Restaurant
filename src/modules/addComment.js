export default (comments =>{
  const commentSec = document.getElementById ('display-comment')
  const input = document.getElementById ('comment-input');
  const inputValue = input.value;
  
  commentSec.innerText = (inputValue);
  getAll = () => comments
}) 

